const images = [
  {height: 10, width: 30},
  {height: 20, width: 90},
  {height: 54, width: 32},
]

const areas = []

// forEach 활용
// images.forEach( () => {})

images.forEach((elem, idx) => {
  // 나만의 로직
  //console.log(elem, idx)
  
})